// ===========================================
// 🧩 Infrastructure/Core/Config.ts
// ===========================================

import { Settings } from "./Config.ts";

// -----------------------------
// Singleton instance (like EnvConfig in Python)
// -----------------------------
export const EnvConfig = new Settings();
